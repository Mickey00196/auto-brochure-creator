# Server-side only — the browser never talks to the backend directly (see
# src/app/api/proxy/[...path]/route.ts). Not prefixed NEXT_PUBLIC_ on
# purpose: it must never ship to client JS.
INTERNAL_API_BASE_URL=http://localhost:8000
